apiVersion: apps/v1
kind: Deployment
metadata:
  name: tns-app
  namespace: tns
  labels:
    app: tns-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tns-app
  template:
    metadata:
      labels:
        app: tns-app
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
        profiles.grafana.com/cpu.scrape: "true"
        profiles.grafana.com/memory.scrape: "true"
        instrumentation.opentelemetry.io/inject-go: "true"
    spec:
      containers:
        - name: tns-app
          image: grafana/tns-app:latest
          args:
            - -log.level=debug
            - http://tns-db.tns.svc.cluster.local:80
          ports:
            - containerPort: 80
          env:
            - name: JAEGER_ENDPOINT
              value: http://tempo.tempo.svc.cluster.local:14268/api/traces
            - name: JAEGER_SAMPLER_TYPE
              value: const
            - name: JAEGER_SAMPLER_PARAM
              value: "1"
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: tns-app
  namespace: tns
spec:
  selector:
    app: tns-app
  ports:
    - port: 80
      targetPort: 80
